PROJECT (HELLO)

cmake_minimum_required(VERSION 3.5)

AUX_SOURCE_DIRECTORY(src SRC_LIST)

MESSAGE(STATUS "This is BINARY dir " ${HELLO_BINARY_DIR})

MESSAGE(STATUS "This is SOURCE dir " ${HELLO_SOURCE_DIR})

ADD_EXECUTABLE(toysort ${SRC_LIST})
